Creating the Quizzr database and the logdata and quizzes tables
__________________________________________________________________________________________________
CREATE DATABASE Quizzr;
USE Quizzr;
CREATE TABLE logdata(Username VARCHAR(20) NOT NULL, Email VARCHAR(60) PRIMARY KEY NOT NULL, Password VARCHAR(999) NOT NULL);
CREATE TABLE quizzes(quizid VARCHAR(20) NOT NULL PRIMARY KEY, quizname VARCHAR(999) NOT NULL, creator VARCHAR(999) NOT NULL);
__________________________________________________________________________________________________

Creating the quizzes database
__________________________________________________________________________________________________
CREATE DATABASE quizzes;






